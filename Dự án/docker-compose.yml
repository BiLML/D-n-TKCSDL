services:
  db:
    image: postgres:15-alpine
    container_name: contentmanagementdb
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - content_network 

  backend:
    build:
      context: ./backend
      dockerfile: dockerfile
    container_name: contentmanagementbackend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
    networks:
      - content_network
    volumes:
      - ./backend/src:/app/src
      - ./backend/alembic:/app/alembic
      - ./backend/alembic.ini:/app/alembic.ini
networks:
  content_network:
    driver: bridge

volumes:
  postgres_data: